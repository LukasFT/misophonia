dataset_splits:
  train: &train
    # from_premade: true
    generated_config:
      num_samples: 2048 # 16384 # 64 samples per shard, * 256 shards
      trig_to_control_ratio: 0.95 # 95% triggers, 5% controls
      backgrounds_per_item: [1, 6]
  val:
    <<: *train
  test:
    <<: *train
  
batch_size: 4

model_params:
  label_len: 8

mlflow_experiment: "model-v1"