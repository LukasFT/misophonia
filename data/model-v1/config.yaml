dataset_base_save_dir: data/
dataset_name: model-v1
dataset_splits:
  train: &train
    # from_premade: true
    generated_config:
      num_samples: 1000
      trig_to_control_ratio: 0.5
  val:
    <<: *train
  test:
    <<: *train
  
  

model_params:
  label_len: 8
